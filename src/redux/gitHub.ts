/*
const initialState = {
    items: [],
    itemsRepos: [],
    user: [],
    userRepos: [],
    isLoading: true
};

export const SET_ITEMS = "SET_ITEMS";
export const SET_USER = "SET_USER";
export const SET_ITEMS_REPOS = "SET_ITEMS_REPOS";
export const SET_USER_REPOS = "SET_USER_REPOS";


const gitHub = (state = initialState, action: any) => {
    switch (action.type) {
        case SET_ITEMS: {
            return {
                ...state,
                items: action.payload,
                isLoading: false
            }
        }
        case SET_USER: {
            return {
                ...state,
                user: action.payload,
                isLoading: false
            }
        }
        case SET_ITEMS_REPOS: {
            return {
                ...state,
                itemsRepos: action.payload,
                isLoading: false
            }
        }
        case SET_USER_REPOS: {
            return {
                ...state,
                userRepos: action.payload,
                isLoading: false
            }
        }
        default: {
            return state;
        }
    }
}

export default gitHub;*/
export {};